// Every B2B appointment happens in the same physical room, so the location is
// not something a participant chooses: it is written on every persona and shown
// read-only in the agenda. Stored in French like the other back-office facing
// fields (pays…), so the organiser sees one spelling for everyone.
export const MEETING_LOCATION = 'Salle de networking';
